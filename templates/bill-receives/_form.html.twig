<form method="post" action="{{ action }}">
    <div class="form-group">
        <label class="control-label">Data de Lançamento</label>
        <input class="form-control" type="text" placeholder="DD/MM/YYYY"
               name="date_launch" value="{{ receive.date_launch|date('d/m/Y') }}">
    </div>
    <div class="form-group">
        <label class="control-label">Nome</label>
        <input class="form-control" type="text" placeholder="Nome"
               name="name" value="{{ receive.name }}">
    </div>
    <div class="form-group">
        <label class="control-label">Valor</label>
        <input class="form-control" type="text" placeholder="Valor"
               name="value" value="{{ receive.value|number_format(2,',','.') }}">
    </div>
    <button type="submit" class="btn btn-primary btn-block">
        <span class="glyphicon glyphicon-{{ submitIcon }}"></span>
    </button>
</form>